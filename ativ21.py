# Crie um programa que utilize uma estrutura de repetição para somar todos os números pares de 1 a 100 e exiba o resultado.
soma = 0

# Loop para iterar pelos números de 1 a 100git init 
for i in range(1, 101):
    # Verifica se o número é par
    if i % 2 == 0:
        soma += i  # Adiciona o número par à soma

# Exibe o resultado
print(f"A soma dos números pares de 1 a 100 é: {soma}")

 Crie um programa que utilize uma estrutura de repetição para somar todos os números pares de 1 a 100 e exiba o resultado.
soma = 0

# Loop para iterar pelos números de 1 a 100git init 
for i in range(1, 101):
    # Verifica se o número é par
    if i % 2 == 0:
        soma += i  # Adiciona o número par à soma

# Exibe o resultado
print(f"A soma dos números pares de 1 a 100 é: {soma}")